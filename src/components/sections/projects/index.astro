---
import Bar from './bar.astro';
import Grid from './grid.astro';

interface Props {
	projects: any;
}
const { projects } = Astro.props;
---

<article class='max-w-normal mx-auto flex flex-col gap-md xl:gap-lg'>
	<Bar />
	<Grid projects={projects} />
</article>
